{
  "name": "alternative12-npo-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "npm run build && node -e \"require('node:fs').writeFileSync('dist/.nojekyll','')\" && gh-pages -d dist --dotfiles",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
